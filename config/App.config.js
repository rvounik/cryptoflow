/**
 * Global App configuration
 */

const AppConfig = {
    api: {
        baseUrl: `http://127.0.0.1:8008`,
        routes: {
            rate: '/rate'
        }
    }
};

export default AppConfig;
